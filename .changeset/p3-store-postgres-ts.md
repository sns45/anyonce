---
"@anyonce/stores": minor
---

Postgres store: PostgresStore over a minimal query adapter (pg directly, fromPostgresJs, fromNeon), ensureSchema and the migration file; the shared begin statement binds precomputed lease and expiry values.
